import React from 'react'

const SettingsListItem = (props) => {
     return (
         <div className="settingsListItem_container">
            {props.children}
         </div>
     )
}

export default SettingsListItem
